@model IEnumerable<PInventario.Models.Producto>

@{
    ViewBag.Title = "IndexInvitado";
}

<h2>Productos</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    
    <tr>
        <th>
            Categoría
        </th>
        <th>
            Marca
        </th>
        <th>
            Producto
        </th>
        <th>lo
            Precio compra
        </th>
        <th>
           Precio Venta
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Categoria.Nombre_categoria)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Marca.Nombre_marca)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Nombre_producto)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Precio_compra)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Precio_venta)
    </td>
    @*<td>
        @Html.DisplayFor(modelItem => item.Lista_movimiento_detalle)
    </td>*@
    @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_producto }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_producto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_producto })
        </td>*@
</tr>
}

</table>
