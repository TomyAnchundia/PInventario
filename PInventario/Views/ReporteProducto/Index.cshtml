@model IEnumerable<PInventario.Models.ReporteProducto>

@{
    ViewBag.Title = "Index";
}
<br />
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item">@Html.ActionLink("Inicio", "Index", "Profile")</li>
        <li class="breadcrumb-item active" aria-current="page">Reporte</li>
    </ol>
</nav>

@*<div class="container-fluid">
    <br />
    <div class="col-md-4">
        <ol class="breadcrumb">
            <li>@Html.ActionLink("Inicio", "Index", "Profile")</li>
            <li class="active">Reporte </li>
        </ol>
    </div>
</div>*@
<h3>
   Nuevo Reporte <a href="@(Url.Action("Create"))"> <img src="~/Content/imagenes/crear.png" alt="Crear" /></a> 

   
</h3>
<table class="table">
    <tr>
        <th>
           Papelería
        </th>
        <th>
            Fecha emisión
        </th>
        <th>
            Descripción
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Papeleria.Nombre_papeleria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_emision)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion_reporte)
            </td>
            <td>
                <a href="@(Url.Action("Edit", new { id = item.Id_reporte_producto }))"> <img src="~/Content/imagenes/editar.png" alt="Editar" /></a> |
                <a href="@(Url.Action("Details", new { id = item.Id_reporte_producto }))"> <img src="~/Content/imagenes/detalles.png" alt="Detalles" /></a> |
                <a href="@(Url.Action("Delete", new { id = item.Id_reporte_producto }))"> <img src="~/Content/imagenes/Eliminar.png" alt="Eliminar" /></a>




            </td>
        </tr>
    }

</table>
